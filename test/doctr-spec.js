var doctr = require('../index.js'),
	fs = require('fs'),
	path = require('path');

describe('doctr', function() {

  it('does exist', function() {
    expect(typeof doctr).toEqual('object');
  });

  it('generates jsdoc from specified input to output folder', function() {

  });

  it('strips unnecessary html-header from generated jsdoc file', function() {

  });

  it('creates a folder `release/doc` for the generated html doc', function() {

  });

  it('makes `release/doc` folder available on a webserver', function() {

  });

});